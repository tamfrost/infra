apiVersion: v1
kind: Service
metadata:
  name: {{ include "df-fix.fullname" . }}
  labels:
    {{- include "df-fix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "df-fix.selectorLabels" . | nindent 4 }}
