apiVersion: v1
kind: Service
metadata:
  name: {{ include "df-nec2.fullname" . }}
  labels:
    {{- include "df-nec2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "df-nec2.selectorLabels" . | nindent 4 }}
