apiVersion: v1
kind: Service
metadata:
  name: cluster-postgres
  namespace: default
spec:
  type: ExternalName
  externalName: postgres.postgres.svc.cluster.local
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
